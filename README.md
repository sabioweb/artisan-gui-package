# artisan-gui-package
Laravel package providing a beautiful web-based GUI for executing Artisan commands. Features include command whitelisting, real-time output, execution history, dark mode support, and full internationalization. Perfect for teams without SSH access.
